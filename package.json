{
  "name": "react-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && npm install && npm run build && cd .. && npm install && node ./bin/www"
  },
  "dependencies": {
    "algoliasearch": "^3.30.0",
    "axios": "^0.18.0",
    "body-parser": "~1.16.0",
    "debug": "~2.6.0",
    "express": "~4.14.1",
    "path": "^0.12.7"
  }
}
